# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
### Added
- **排查模板参数芯片可点击插入** — 编辑动作时点一下参数芯片，`${参数}` 就插入到输入框光标处，不用再手敲

### Security
- **修复恶意日志内容可在查看器中执行脚本的问题** — 日志内容现在一律按纯文本处理，导入排查模板也会先做完整校验
- **查看器默认禁止对外联网** — 作为兜底，即使防护被绕过，日志内容也无法向外发送数据

### Fixed
- **修复删除/裁剪日志中途失败会丢失整个文件的问题** — 比如文件被其他程序占用导致操作中断，现在原文件始终保持完好
- **修复纯日期删除/保留的边界偏移 8 小时的问题** — 输入 `2024-01-01` 现在按当天本地 0 点计算
- **修复命令面板「显示统计」「跳转到行」无响应的问题**
- **修复分屏多开时删除/裁剪可能作用于另一个文件的问题** — 现在固定作用于正在查看的文件
- **修复文件读取失败后永远卡在「加载中」的问题** — 现在会提示错误，并可直接点「刷新」重试
- **修复跳转后分页错乱的问题** — 总页数不再显示上万页、翻过去全是空白页，跳转后筛选也不再漏掉大部分日志
- **修复折叠模式改「每页行数」后「下一页」点击无响应的问题**
- **修复筛选/跳转的过期结果覆盖当前画面的问题** — 取消筛选后级别勾选恢复为全选
- **修复搜索/筛选后跳转时合法行号被误报「超出范围」的问题**
- **修复注释输入框、下拉框的方向键被翻页快捷键拦截的问题**
- **修复正则搜索命中结果不高亮的问题**
- **修复删除/裁剪时输入提示不显示的问题**
- **修复「防抖间隔」设为 0 不生效的问题**；回车搜索也不再重复搜索、把页码拽回第 1 页
- **修复删除/裁剪改写文件换行格式的问题** — 保留原有的 Windows 换行符
- **修复加载时按钮被撑大的问题**

### Performance
- **大文件搜索不再卡顿界面**
- **书签、注释列表打开不再卡死** — 文件上千万行、条目较多时也能秒开
- **跳转和时间线打开更快** — 文件没变过时直接复用上次结果
- **导出大量日志时内存占用更低**
- **统计不再被脏日志撑爆** — 日志混有网址、版本号等内容时照常可用
- **排查模板编辑更可靠** — 关键词空格按「空格分隔」规则保存；JSON 切回表单先校验，不再让编辑器出错

## [1.3.3] - 2026-09-21
### Fixed
- **排查模板运行时没填的参数按空处理** — 模板里声明了的参数，运行时留空的话，动作里的 `${参数}` 位置不再原样显示参数名，而是按空处理：搜索关键词因此全空时走该动作配置的「0 条命中」策略，筛选/注释为空时提示跳过，书签名回退到默认的「行 N」
- **折叠模式下搜索/筛选后页数不刷新、出现空白页** — 折叠浏览到第 N 页后搜索或筛选，**总页数仍显示 N 页**，翻到后面几页会完全空白
- **折叠模式下「跳转到完整日志」定位错位** — 折叠 + 搜索后点击行首按钮落到错误位置
- **折叠模式下跳转完整日志后折叠效果失效** — 跳转时折叠其实是保持的，但异步重算没算到目标页导致走标准分页、看不到折叠徽标；现在等折叠页范围算完再跳转，折叠视觉保持
- **搜索后删改搜索词时历史下拉不弹出** — 执行过一次搜索后历史下拉会收起，此时继续删除或修改搜索词，下拉不会重新出现，要点一下输入框才行；现在输入内容一有变化，历史下拉就跟着弹出并实时过滤，输入停顿后的自动搜索也不会再把它收起
### Added
- **高级搜索关键词输入框支持搜索历史** — 聚焦高级搜索里的「关键词」输入框也会弹出历史下拉，与首页搜索框共用同一份记录（在任一处实际搜过的关键词都会出现在另一处）；单条删除、一键清空行为一致；下拉弹出/收起都按 Esc 优先关闭下拉而不是关弹窗
- **支持自定义日志级别缩写** — 像 `I` / `E` / `W` 这种简写现在能识别为 INFO / ERROR / WARN，也可识别 `ERR` / `WRN` / `INF` / `DBG` / `TRC` 等多字符变体。在「设置 → 扩展 → 大日志文件查看器 → Level Aliases」里调整（配置键 `big-log-viewer.level.aliases`）。缩写仅在日志头部的时间戳之后或方括号内识别，不会把日志正文里的 `I/O`、英文 `I` 误判为级别
  - **升级提示**：之前识别为「其他」的行（如 `10:00:00 I start`）现在会归到对应级别，统计面板里的级别数字会变（对应级别增多、「其他」减少）。这是预期行为，不是 bug；配置变更或升级后需要重新打开或刷新日志文件才生效

## [1.3.2] - 2026-09-11
### Changed
- **高级搜索改为查询构建器** — 每个条件一行（字段 + 匹配方式 + 值），用「且/或」芯片控制整体逻辑，底部有实时查询预览告诉你当前在搜什么
### Added
- **搜索历史** — 重新聚焦搜索框可弹出历史下拉，点击条目一键重搜；正则条目带 `.*` 标识；支持单条删除与一键清空；按文件隔离、当前会话有效

## [1.3.1] - 2026-09-10
### Changed
- **排查模板编辑器改成「流水线」风格** — 步骤以带连接线的轨道呈现，收起时可一眼通读整条流程；展开编辑同时只展开一张
- **书签行一眼能看出来** — 整行加深背景 + 行首书签色条，配色跟随主题（默认琥珀、NEON.CYBER 酸性黄、AURORA.GLASS 极光粉、HOLO.PRISM 霓虹粉）

## [1.3.0] - 2026-09-04
### Added
- **新增「排查模板」功能** — 把固定排查流程保存为可复用模板:模板 = 运行参数 + 线性动作序列(查询日志/筛选/打书签/加注释),运行时自上而下自动执行并在动作间传递上下文(如从命中行自动提取线程名并筛选),查到/筛到哪就自动定位到哪。查询动作支持「从上一步命中之后开始查」「仅同线程日志」两个约束,可链式定位「上一条日志之后的下一处匹配」;命中策略决定本步产出集(让我选择/自动选第一条 → 单条,保留全部 → 整批),查询动作另有空结果策略(终止/跳过继续)。可视化/JSON 双形式编辑,执行中有步骤进度并可随时停止,结束后显示摘要。编辑模板时可一键收起编辑器、复制日志后自动恢复,方便边看日志边填模板。模板可通过剪贴板或 JSON 文件导入导出(按 id 去重合并),方便分享给同事。入口:工具栏「排查模板」按钮或命令面板「日志查看器: 排查模板」
### Fixed
- **修复右键「移除书签」误弹书签列表** — 在已添加书签的日志行上右键选择「移除书签」后,不会再无端弹出书签管理弹框,直接静默移除并提示

## [1.2.9] - 2026-08-04
### Added
- **书签支持自定义名称** — 右键日志行「添加书签」时弹出输入框,可为书签起一个易记的名字(如「第一次执行」);留空则回退到默认名称「行 N」。双击日志行仍然是快速添加/移除,使用默认名称
### Changed
- **移除右键菜单中的「定位到第 N 行」** — 既然能右键点中该行就已可见,无需再单独定位,菜单更精简
### Fixed
- **修复注释删除按钮点击无反应** — 注释管理弹窗里和日志行右键菜单中的「删除注释」均恢复正常,删除前会有确认提示

## [1.2.8] - 2026-06-24
### Fixed
- **跳转到行号在超大文件上卡死/内存溢出** — 现在秒级响应
- **按时间定位偶发卡死** — 命中目标后能正常返回
- **加载进度条在大文件上永远卡 99%** — 进度按真实已读字节计算，大文件平滑推进
- **错误提示偶发乱码字符** — 现在能正常显示
- **Java 类名包含中文时识别失败** — 现在能正确识别（如 `中文.示例类`）

## [1.2.7] - 2026-06-08
### Added
- **新增切换主题功能** — 在「设置」面板里可切换默认(跟随 VSCode) / NEON.CYBER / AURORA.GLASS / HOLO.PRISM,立即生效。也可在 `big-log-viewer.theme` 中配置。

### Fixed
- 修复打开日志文件时进度条卡 0%、一直停留在加载页面、首次打开闪一下的问题

## [1.2.6] - 2026-04-21
### Fixed
- **日志打开时偶发跳到一万多行 / 显示不完整 / 页数算错** — 初始加载与后台加载的状态一致性修复

## [1.2.5] - 2026-04-20
### Added
- **日志裁剪支持两种保留方式** — 按时间范围保留指定时段、按行号范围保留指定行；其余部分删除

### Fixed
- **筛选线程不生效 / 搜索后筛选失效** — 两者叠加使用现在工作正常
- **取消筛选后没回到原位置** — 现在能正确恢复

## [1.2.4] - 2026-03-18
### Added
- **回到顶部按钮** — 向下滚动时右下角出现浮动按钮，点击快速回到当前页顶部

### Fixed
- **点击线程名/方法名高亮标签后的筛选按钮无响应** — 现在能正常按线程名或方法名筛选日志

## [1.2.3] - 2026-01-27
### Changed
- **界面更简洁专业** — 按钮图标统一为 VSCode 原生风格；线程/类/方法名标签改成更清爽的轮廓样式，只保留日志级别的实心高亮
- **支持键盘导航、触屏、动画偏好、高对比度模式** — 沿用系统辅助功能设置，不擅自加动画或低对比度
- **工具栏布局调整** — 「设置」按钮移入「⋯ 更多」菜单，主工具栏只留常用功能（定位、书签、注释、高亮规则）
### Fixed
- **搜索结果跳转到完整日志后内容没刷新** — 显示「已跳转」但页面没换，现在正确切到完整日志视图

## [1.2.2] - 2026-01-22
### Added
- **支持同时打开多个日志文件** — 每个文件独立面板，可来回切换；面板标题显示文件名
- **加载进度提示** — 大文件打开时先显示全屏加载条，界面可用后右下角小条继续显示后台加载进度，可随时暂停
- **导出书签日志** — 在书签管理弹窗里可一键导出所有带书签的日志到文件（按行号排序，默认文件名 `bookmarked.log`）
- **多个过滤条件可以叠加生效** — 关键词 + 线程名 + 级别等同时生效，例如先按线程名筛再输入关键词，两条件都起作用
- **「当前页」搜索** — 搜索框旁勾「当前页」可只搜当前页内容，速度更快；可一键退出

### Changed
- **搜索采用统一过滤系统** — 多个条件叠加生效，过滤体验更连贯

### Removed
- **移除反向搜索** — 如需排除特定内容，用日志级别过滤或高级搜索代替

### Fixed
- **搜索/过滤后书签显示「（已不存在）」** — 书签列表改为从完整数据中查找，导出书签日志也同步修复
- **设置按钮点击无反应** — 现在能正常打开设置面板
- **`[ERROR]` 等日志级别被错误识别为线程名** — 线程名高亮规则现在只匹配真正的线程名
- 搜索、线程名筛选、级别过滤三者互相覆盖的问题

## [1.2.1] - 2026-01-16
### Fixed
- **HTML 代码被当成文本显示** — 之前日志里出现 HTML 标签会显示源码，现在能正常渲染高亮和图标
- **搜索结果跳转重复加载数据** — 数据已加载时直接跳转，避免页码错位；未加载时才请求后端
- **搜索跳转响应慢** — 智能判断数据状态，秒级响应

## [1.2.0] - 2026-01-14
### Changed
- 多关键词搜索现在是默认搜索方式，无需勾选复选框
- 移除了"多关键词"复选框，简化界面操作
- 搜索框提示文字更新为"搜索日志内容（多关键词用空格分隔）"，更清晰地说明使用方式
- 正则模式下自动禁用多关键词匹配，避免冲突

### Enhanced
- 高级搜索功能全面升级
  - 支持添加多个搜索条件，可自由组合
  - 支持 AND（且）/ OR（或）逻辑选择
  - 新增字段级搜索：线程名、类名、方法名
  - 线程名/类名/方法名支持精确匹配和模糊匹配两种模式
  - 优化搜索界面，条件管理更直观
  - 每个条件可独立配置和删除

## [1.1.5] - 2026-01-12
### Added
- 新增多关键词搜索功能，支持通过空格分隔的多个关键词进行 AND 逻辑匹配，并支持对应的高亮显示

### Fixed
- 修复时间线加载不完整的问题
- 优化加载日志逻辑，提升用户体验
- 修复折叠重复日志模式下加载进度问题

### Changed
- 搜索框支持输入后自动触发搜索（带轻微防抖），可在设置中调整防抖时间
- 折叠重复日志的徽标会显示当前搜索命中的条数，便于快速判断重要性
- 新增设置面板，支持配置搜索防抖时间、折叠最小重复次数、时间线采样点数
- 统一后台增量加载逻辑，时间线始终基于整文件采样并在跳转后保持正确的当前位置高亮

## [1.1.4] - 2025-12-16
### Added
- 新增若干命令，包括以下命令
  - 刷新日志文件
  - 导出当前日志
  - 显示日志统计
  - 书签管理
  - 注释管理
  - 跳转到行号
  - 高级搜索
- 搜索结果导航优化
  - 搜索结果添加跳转按钮（图标），点击可跳转到完整日志中的对应位置
  - 右键菜单新增"跳转到完整日志"选项，方便在搜索模式下快速定位
  - 取消搜索结果点击自动跳转功能，便于对比和复制内容
- 时间线导航增强
  - 时间线图上高亮显示当前浏览位置对应的时间块
  - 翻页或跳转时自动更新高亮块位置
  - 点击时间线跳转后，高亮块准确定位到目标位置
  - 基于实际时间戳计算高亮位置，确保与日志内容精确对应

### Fixed
- 修复搜索功能数据加载问题
  - 修复搜索时出现错误
  - 添加防御性检查，确保搜索结果数据始终为数组类型
  - 修复搜索结果为空时的提示信息
- 修复搜索结果显示问题
  - 修复搜索结果下方仍显示其他日志的问题
  - 防止在搜索/过滤模式下自动加载更多数据
  - 搜索结果现在只显示匹配的日志行

## [1.1.3] - 2025-12-09

### Added
- JSON自动解析支持多对象检测和渲染
  - 单行日志包含多个JSON对象时，自动分别解析展示
  - 保留日志前缀的原有格式和高亮样式
  - 每个JSON对象独立控制折叠/展开
- JSON折叠/展开交互优化
  - 修复折叠按钮显示逻辑，避免重复的结束括号
  - 支持展开后重新折叠，状态切换更加流畅
  - 优化嵌套JSON的渲染，只在根层级提供折叠控件

### Fixed
- 修复JSON解析时嵌套内容被意外折叠的问题
- 修复多JSON渲染时日志前缀格式丢失的问题
- 修复折叠/展开切换时DOM结构不一致的问题

## [1.1.2] - 2025-12-04

### Added
- JSON/XML自动解析功能
  - 自动检测并美化渲染日志中的JSON对象
  - 自动检测并美化渲染日志中的XML内容
  - 支持大型JSON的智能折叠（超过20行自动折叠）
  - 可通过开关控制是否启用自动解析
- 自定义高亮规则系统
  - 支持添加、编辑、删除自定义高亮规则
  - 支持文本精确匹配和正则表达式两种模式
  - 自定义背景色和文字颜色
  - 高亮内容可一键筛选日志
- 日志筛选功能增强
  - 支持从高亮内容快速筛选
  - 显示筛选状态提示
  - 支持取消筛选并恢复原始位置

### Changed
- 改进JSON提取算法，使用括号匹配代替正则表达式
- 优化折叠重复日志的性能
- 优化虚拟滚动渲染效率

### Fixed
- 修复JSON解析不完整的问题
- 修复嵌套JSON折叠逻辑错误

## [1.1.0] - 2025-12-03

### Added
- 虚拟滚动支持超大文件（GB级）秒开
- 智能分页加载机制
- 日志级别过滤（Error、Warn、Info、Debug）
- 时间范围过滤
- 折叠重复日志功能
- 书签标注功能
- 注释添加功能

### Added
- 多条件组合搜索
  - 支持正则表达式搜索
  - 支持反向搜索（排除匹配）
  - 支持多个搜索条件同时生效
- 搜索结果高亮显示
- 搜索结果计数

### Added
- 按时间删除日志
- 按行数删除日志
- 批量删除操作

### Added
- 日志统计信息
- 时间线导航
- 日志导出功能

### Changed
- 内置高亮规则优化
  - 时间戳高亮
  - 日志级别颜色区分
  - 线程名高亮
  - 类名高亮
  - 方法名高亮
- 暗色主题适配

## [1.0.0] - 2025-11-25

### Added
- 初始版本发布
- 基础日志查看功能
- 文件加载和显示
- 简单搜索功能
- 基本的界面布局